diff --git a/package/mtk/applications/luci-app-eqos-mtk/root/etc/init.d/eqos b/package/mtk/applications/luci-app-eqos-mtk/root/etc/init.d/eqos
index 7b454c44a5..5f3b4c49f5 100755
--- a/package/mtk/applications/luci-app-eqos-mtk/root/etc/init.d/eqos
+++ b/package/mtk/applications/luci-app-eqos-mtk/root/etc/init.d/eqos
@@ -41,14 +41,10 @@ start_service() {
         config_get_bool ipv6enabled "config" "ipv6enabled" "0"    
         ip6tables -t mangle -N eqos
         ip6tables -t mangle -F eqos
-        ebtables -t nat  -N eqos
-        ebtables -t nat  -F eqos
-        if [ "$ipv6enabled" -eq "1" ]; then                                
-        ebtables -t nat -D POSTROUTING  -j eqos -p ipv6              
-        ebtables -t nat -A POSTROUTING  -j eqos -p ipv6
-        ip6tables -t mangle -D FORWARD  -j eqos
-        ip6tables -t mangle -A FORWARD  -j eqos             
-        fi  
+        if [ "$ipv6enabled" -eq "1" ]; then
+            ip6tables -t mangle -D FORWARD  -j eqos
+            ip6tables -t mangle -A FORWARD  -j eqos
+        fi
         
         if [ "$smarthqos" -eq "1" ]; then  
         # 上行 per-user 队列: Q5-Q29 (跳过 Q0-Q4 优先队列, Q30 背景, Q31 限速)
diff --git a/package/mtk/applications/luci-app-eqos-mtk/root/usr/sbin/eqos b/package/mtk/applications/luci-app-eqos-mtk/root/usr/sbin/eqos
index 9c043e9642..7f6eb54baf 100755
--- a/package/mtk/applications/luci-app-eqos-mtk/root/usr/sbin/eqos
+++ b/package/mtk/applications/luci-app-eqos-mtk/root/usr/sbin/eqos
@@ -29,7 +29,10 @@ start_qos() {
 iptables_stop() {
     iptables -t mangle -D FORWARD  -j eqos
     ip6tables -t mangle -D FORWARD  -j eqos
-    ebtables -t nat -D POSTROUTING  -j eqos -p ipv6
+    iptables -t mangle -D FORWARD  -j eqos_apply 2>/dev/null
+    ip6tables -t mangle -D FORWARD  -j eqos_apply 2>/dev/null
+    iptables -t mangle -D FORWARD -m conntrack --ctstate ESTABLISHED,RELATED -j CONNMARK --restore-mark --nfmask 0xFF --ctmask 0xFF 2>/dev/null
+    ip6tables -t mangle -D FORWARD -m conntrack --ctstate ESTABLISHED,RELATED -j CONNMARK --restore-mark --nfmask 0xFF --ctmask 0xFF 2>/dev/null
     echo 0 > /sys/kernel/debug/hnat/qos_toggle
     sed -i '/\/etc\/init.d\/eqos start/d' /etc/firewall.user
     # 清理 dscp_clear 链
@@ -72,13 +75,42 @@ iptables_start_prepare() {
     ip6tables -t mangle -A dscp_clear -m dscp --dscp 46 -j DSCP --set-dscp 44
     ip6tables -t mangle -D FORWARD -j dscp_clear 2>/dev/null
     ip6tables -t mangle -I FORWARD 1 -j dscp_clear
+
+    # 新架构: 统一连接跟踪标记还原 (仅还原低8位 0xFF，保护 mwan3 等多线高位 mark)
+    iptables -t mangle -D FORWARD -m conntrack --ctstate ESTABLISHED,RELATED -j CONNMARK --restore-mark --nfmask 0xFF --ctmask 0xFF 2>/dev/null
+    iptables -t mangle -I FORWARD 1 -m conntrack --ctstate ESTABLISHED,RELATED -j CONNMARK --restore-mark --nfmask 0xFF --ctmask 0xFF
+    ip6tables -t mangle -D FORWARD -m conntrack --ctstate ESTABLISHED,RELATED -j CONNMARK --restore-mark --nfmask 0xFF --ctmask 0xFF 2>/dev/null
+    ip6tables -t mangle -I FORWARD 1 -m conntrack --ctstate ESTABLISHED,RELATED -j CONNMARK --restore-mark --nfmask 0xFF --ctmask 0xFF
 }
 
 iptables_start_inital() {
     iptables -t mangle -N eqos
+    iptables -t mangle -N eqos_apply 2>/dev/null
     iptables -t mangle -F eqos
+    iptables -t mangle -F eqos_apply 2>/dev/null
     iptables -t mangle -D FORWARD  -j eqos
+    iptables -t mangle -D FORWARD  -j eqos_apply 2>/dev/null
     iptables -t mangle -A FORWARD  -j eqos
+    iptables -t mangle -A FORWARD  -j eqos_apply
+
+    ip6tables -t mangle -N eqos 2>/dev/null
+    ip6tables -t mangle -N eqos_apply 2>/dev/null
+    ip6tables -t mangle -F eqos 2>/dev/null
+    ip6tables -t mangle -F eqos_apply 2>/dev/null
+    ip6tables -t mangle -A FORWARD  -j eqos 2>/dev/null
+    ip6tables -t mangle -A FORWARD  -j eqos_apply 2>/dev/null
+
+    # 全局应用链 (eqos_apply): 在 eqos 链完成 set-xmark 之后执行
+    # 这一步极其关键: CONNMARK --set-xmark 仅仅修改了内存表中的状态，并没有同步到当前这第一个数据包上。
+    # 必须通过 restore-mark 强制将刚刚打上的状态赋给当前 skb->mark，否则第一包(也是触发硬件加速编程的包)将漏判！
+    iptables -t mangle -A eqos_apply -m conntrack --ctstate ESTABLISHED,RELATED -j CONNMARK --restore-mark --nfmask 0xFF --ctmask 0xFF
+    ip6tables -t mangle -A eqos_apply -m conntrack --ctstate ESTABLISHED,RELATED -j CONNMARK --restore-mark --nfmask 0xFF --ctmask 0xFF
+
+    # 全局 DSCP 同步规则: 根据 CONNMARK 还原后的 skb->mark (匹配低8位) 自动同步 DSCP 给软件队列使用
+    iptables -t mangle -A eqos_apply -m mark --mark 46/0xFF -j DSCP --set-dscp 46
+    ip6tables -t mangle -A eqos_apply -m mark --mark 46/0xFF -j DSCP --set-dscp 46
+    iptables -t mangle -A eqos_apply -m mark --mark 2/0xFF -j DSCP --set-dscp 2
+    ip6tables -t mangle -A eqos_apply -m mark --mark 2/0xFF -j DSCP --set-dscp 2
     echo 1 > /sys/kernel/debug/hnat/qos_toggle
     # 基线调度器配置 (后续 start 会完整覆盖)
     echo 0 sp 10000000 > /sys/kernel/debug/hnat/qdma_sch0
@@ -170,14 +202,9 @@ case "$1" in
     # 偏移 0xc = 源 IP 起始 (字节 12-15 = 192.168.x.y)
     # &0x0000FF00 掩码提取第三字节 x, =0x6E00:0x7700 即 110-119
     # &0x000000FF 掩码提取第四字节 y, =0x0A:0x27 即 10-39
-    iptables -t mangle -I eqos -s 192.168.0.0/16 -m u32 --u32 "0xc&0x0000FF00=0x00006E00:0x00007700" -m u32 --u32 "0xc&0x000000FF=0x0000000A:0x00000027" -j DSCP --set-dscp 46
-
-    # 目标IP: 192.168.x.y, x=[110-119], y=[10-39]
-    # 偏移 0x10 = 目标 IP 起始 (字节 16-19)
-    # 同样使用位掩码提取第三、四字节
-    iptables -t mangle -I eqos -d 192.168.0.0/16 -m u32 --u32 "0x10&0x0000FF00=0x00006E00:0x00007700" -m u32 --u32 "0x10&0x000000FF=0x0000000A:0x00000027" -j DSCP --set-dscp 46
-    iptables -t mangle -I eqos -d 192.168.0.0/16 -m u32 --u32 "0x10&0x0000FF00=0x00006E00:0x00007700" -m u32 --u32 "0x10&0x000000FF=0x0000000A:0x00000027" -j MARK --set-mark 46
-    logger -p daemon.debug -t eqos "[EQOS-C-FQOS01-02-05] install trusted static VIP download marker: dst=192.168.110-119.10-39, mark=46, queue=32"
+    # 新架构: 使用 CONNMARK, 只需要抓取上传方向(源IP), CONNMARK 会自动标记整个双向连接 (加 0xFF 掩码保护)
+    iptables -t mangle -I eqos -s 192.168.0.0/16 -m u32 --u32 "0xc&0x0000FF00=0x00006E00:0x00007700" -m u32 --u32 "0xc&0x000000FF=0x0000000A:0x00000027" -j CONNMARK --set-xmark 46/0xFF
+    logger -p daemon.debug -t eqos "[EQOS-C-FQOS01-02-05] install unified static VIP marker: src=192.168.110-119.10-39, connmark=46"
 
     # 192.168.109.0/24 网段: 仅加速小包 UDP（游戏包）
     # 条件: UDP 协议 + 包长度 ≤ 300 字节
@@ -228,30 +255,21 @@ case "$1" in
     esac
 
     if [ "$id" -eq 64 ]; then
+        # 清理旧规则
+        iptables -t mangle -D eqos -m mac --mac-source $macaddr -j CONNMARK --set-xmark 46/0xFF 2>/dev/null
+        ip6tables -t mangle -D eqos -m mac --mac-source $macaddr -j CONNMARK --set-xmark 46/0xFF 2>/dev/null
 
-        iptables -t mangle -D eqos -s $ip -j DSCP --set-dscp 46 2>/dev/null
-        iptables -t mangle -D eqos -d $ip -j DSCP --set-dscp 46 2>/dev/null
-        iptables -t mangle -D eqos -d $ip -j MARK --set-mark 46 2>/dev/null
-        # IPv6 上行使用 DSCP, HNAT dscp_en 按 IPv6 Traffic Class 选择队列。
-        ip6tables -t mangle -D eqos -m mac --mac-source $macaddr -j DSCP --set-dscp 46 2>/dev/null
-        ebtables -t nat -D eqos -p ipv6 -d $macaddr -j mark --mark-set 46 2>/dev/null
-
-        iptables -t mangle -A eqos -s $ip -j DSCP --set-dscp 46
-        ip6tables -t mangle -A eqos -m mac --mac-source $macaddr -j DSCP --set-dscp 46
-
-        iptables -t mangle -A eqos -d $ip -j DSCP --set-dscp 46
-        iptables -t mangle -A eqos -d $ip -j MARK --set-mark 46
-        ebtables -t nat -A eqos -p ipv6 -d $macaddr -j mark --mark-set 46
-        logger -p daemon.debug -t eqos "[EQOS-C-FQOS01-02-04] install trusted VIP download marker: ip=$ip, mac=$macaddr, mark=46, queue=32"
+        # 重新应用新规则 (带掩码保护高位 mark)
+        iptables -t mangle -A eqos -m mac --mac-source $macaddr -j CONNMARK --set-xmark 46/0xFF
+        ip6tables -t mangle -A eqos -m mac --mac-source $macaddr -j CONNMARK --set-xmark 46/0xFF
+        logger -p daemon.debug -t eqos "[EQOS-C-FQOS01-02-04] install unified VIP marker: mac=$macaddr, connmark=46"
 
     elif [ "$id" -lt 64 ]; then
-        # 限速设备: 强制标记 DSCP 2 → 内核映射到 Q31/Q63 (硬件限速队列)
+        # 限速设备: 强制标记 CONNMARK 2 → HNAT 识别为 mark 2 → 映射到 Q31/Q63 (硬件限速队列)
+        # 全局规则会自动将 mark 2 转换为 DSCP 2 供 CAKE 软件队列使用
         # 删除旧规则时, 先从累加器减去该设备的速率。
-        iptables -t mangle -D eqos -s $ip -j DSCP --set-dscp 2 2>/dev/null
-        iptables -t mangle -D eqos -d $ip -j DSCP --set-dscp 2 2>/dev/null
-        # IPv6 上行必须使用 DSCP, 避免 HNAT dscp_en 路径忽略 MARK。
-        ip6tables -t mangle -D eqos -m mac --mac-source $macaddr -j DSCP --set-dscp 2 2>/dev/null
-        ebtables -t nat -D eqos -p ipv6 -d $macaddr -j mark --mark-set 2 2>/dev/null
+        iptables -t mangle -D eqos -m mac --mac-source $macaddr -j CONNMARK --set-xmark 2/0xFF 2>/dev/null
+        ip6tables -t mangle -D eqos -m mac --mac-source $macaddr -j CONNMARK --set-xmark 2/0xFF 2>/dev/null
 
         # 如果删除的规则存在(即重新添加同一设备), 需要先减去旧速率
         # 检查该设备是否已有记录, 如有则减去其旧速率后再加新速率
@@ -289,10 +307,13 @@ case "$1" in
         echo $up > "$old_up_file"
         echo $dl > "$old_dl_file"
 
+        if [ $up -ne 0 ] || [ $dl -ne 0 ]; then
+            iptables -t mangle -A eqos -m mac --mac-source $macaddr -j CONNMARK --set-xmark 2/0xFF
+            ip6tables -t mangle -A eqos -m mac --mac-source $macaddr -j CONNMARK --set-xmark 2/0xFF
+            logger -p daemon.debug -t eqos "[EQOS-C-FQOS01-02-06] install unified limit marker: mac=$macaddr, connmark=2/0xFF, up=${up}, dl=${dl}"
+        fi
+
         if [ $up -ne 0 ]; then
-            iptables -t mangle -A eqos -s $ip -j DSCP --set-dscp 2
-            ip6tables -t mangle -A eqos -m mac --mac-source $macaddr -j DSCP --set-dscp 2
-            logger -p daemon.debug -t eqos "[EQOS-C-FQOS01-02-03] install limit upload marker: ip=$ip, mac=$macaddr, mode=$id, dscp=2, queue=31, rate=${up}"
             cur_up=$(cat /tmp/eqos_q31_rate 2>/dev/null || echo 0)
             new_up=$((cur_up + up))
             echo $new_up > /tmp/eqos_q31_rate
@@ -300,9 +321,6 @@ case "$1" in
         fi
 
         if [ $dl -ne 0 ]; then
-            iptables -t mangle -A eqos -d $ip -j DSCP --set-dscp 2
-            ebtables -t nat -A eqos -p ipv6 -d $macaddr -j mark --mark-set 2
-            logger -p daemon.debug -t eqos "[EQOS-C-FQOS01-02-06] install limit download marker: ip=$ip, mac=$macaddr, mode=$id, dscp=2, mark=2, queue=63, rate=${dl}"
             cur_dl=$(cat /tmp/eqos_q63_rate 2>/dev/null || echo 0)
             new_dl=$((cur_dl + dl))
             echo $new_dl > /tmp/eqos_q63_rate
diff --git a/target/linux/mediatek/files-5.4/drivers/net/ethernet/mediatek/mtk_hnat/hnat_nf_hook.c b/target/linux/mediatek/files-5.4/drivers/net/ethernet/mediatek/mtk_hnat/hnat_nf_hook.c
index 042e6db263..7899e04aef 100644
--- a/target/linux/mediatek/files-5.4/drivers/net/ethernet/mediatek/mtk_hnat/hnat_nf_hook.c
+++ b/target/linux/mediatek/files-5.4/drivers/net/ethernet/mediatek/mtk_hnat/hnat_nf_hook.c
@@ -2047,7 +2047,8 @@ static unsigned int skb_to_hnat_info(struct sk_buff *skb,
         hash_ip = (dir == HQOS_DOWNLOAD) ? new_dip_val : orig_sip;
     }
 
-    qos_mark = skb->mark;  // 提取完整 mark 进行精确匹配，防止高位 mark 碰撞 VIP/限速判断
+    // 提取完整 QoS mark (仅匹配低8位)，防止受到多线路由 (mwan3 等) 高位 mark 的干扰
+    qos_mark = skb->mark & 0xFF;
 
     if (dir == HQOS_DOWNLOAD) {
         if (skb->protocol == htons(ETH_P_IP)) {
diff --git a/task/CHANGE_INDEX.md b/task/CHANGE_INDEX.md
index 8afc3d3b76..86e626f8e9 100644
--- a/task/CHANGE_INDEX.md
+++ b/task/CHANGE_INDEX.md
@@ -21,9 +21,12 @@
   | C-FQOS01-01 | pending | - | 2026-05-02 | MT798x HNAT datapath dead-code cleanup, syntax fix, and fallback QoS refactoring | ✅ 已完成 |
   | C-FQOS01-02 | pending | - | 2026-05-02 | Fix QoS regression findings for IPv6 limit DSCP/mark, trusted VIP download Q32 marker, zero-speed limit cancellation, exact mark matching, legacy mode parsing, CAKE highest tin guard, and verification queue labels | ✅ 已完成 |
   | C-FQOS01-03 | pending | - | 2026-05-02 | Refactor DOWNLOAD QoS marking logic to cleanly isolate IPv4 and IPv6 rule sets | ✅ 已完成 |
+  | C-FQOS01-04 | pending | - | 2026-05-02 | Refactor eqos ruleset to a unified CONNMARK architecture, solving IPv6 HNAT bypass | ✅ 已完成 |
 - Bugs:
   | BID | 描述 | 引入者 | 修复者 | 状态 |
   |-----|------|--------|--------|------|
   | B-001 | QoS 回归审计修复 | C-FQOS01-01 | C-FQOS01-02 | 已修复 |
+  | B-002 | CONNMARK 高位掩码防 mwan3 冲突 | C-FQOS01-04 | C-FQOS01-04 | 已修复 |
+  | B-003 | 第一包漏判导致硬件卸载队列错误 | C-FQOS01-04 | C-FQOS01-04 | 已修复 |
 
 <!-- 新增 Feature 在此下方添加 -->
diff --git a/task/TASK_LOG.md b/task/TASK_LOG.md
index 3067b43caf..581589d78a 100644
--- a/task/TASK_LOG.md
+++ b/task/TASK_LOG.md
@@ -43,3 +43,37 @@
 - **测试同步**: ✅ test/qos-test.md
 
 <!-- 新增 commit 记录在此下方添加 -->
+
+## [2026-05-02] C-FQOS01-04 | commit: d6f47e1fce
+
+- **FID**: F-QOS01
+- **CID**: C-FQOS01-04
+- **类型**: refactor
+- **范围**: luci-app-eqos-mtk, tests
+- **描述**: Refactor eqos ruleset to a unified CONNMARK architecture. Replaced all ebtables and IP-based DSCP rules with global CONNMARK tracking. Solved IPv6 hardware offload bypass issue and unified QoS tracking for both IPv4 and IPv6.
+- **改动文件**: root/usr/sbin/eqos, root/etc/init.d/eqos, test/qos-regression.sh
+- **PRD 同步**: ✅ PRD/qos-hardening.md
+- **方案同步**: ✅ design/qos-architecture.md
+- **测试同步**: ✅ test/qos-regression.sh
+
+## [2026-05-02] B-002 | commit: d6f47e1fce
+
+- **BID**: B-002
+- **关联 FID**: F-QOS01
+- **类型**: bugfix
+- **范围**: luci-app-eqos-mtk, mtk_hnat
+- **描述**: Add `0xFF` mask to `CONNMARK` operations and `skb->mark & 0xFF` kernel extraction to prevent catastrophic collision with `mwan3` multi-WAN routing marks (which use bits 8-13).
+- **改动文件**: root/usr/sbin/eqos, hnat_nf_hook.c, test/qos-regression.sh
+- **测试同步**: ✅ test/qos-regression.sh
+
+## [2026-05-02] B-003 | commit: 7b3380f9fd
+
+- **BID**: B-003
+- **关联 FID**: F-QOS01
+- **类型**: bugfix
+- **范围**: luci-app-eqos-mtk
+- **描述**: Fix first-packet hardware acceleration bypass. `CONNMARK --set-xmark` only sets the connection mark, leaving `skb->mark` as 0 for the first packet. This caused HNAT to program FOE to the wrong queue (Q33). Introduced `eqos_apply` chain to enforce `restore-mark` and DSCP sync after the connection mark is set.
+- **改动文件**: root/usr/sbin/eqos, test/qos-regression.sh
+- **测试同步**: ✅ test/qos-regression.sh
+
+<!-- 新增 commit 记录在此下方添加 -->
diff --git a/test/qos-regression.sh b/test/qos-regression.sh
index 4bc9cd1d74..b6bb1c03dc 100755
--- a/test/qos-regression.sh
+++ b/test/qos-regression.sh
@@ -24,35 +24,31 @@ grep -Fq 'case "$dl" in' "$EQOS" ||
 grep -Fq 'case "$up" in' "$EQOS" ||
     fail "upload speed must be normalized before numeric comparisons"
 
-grep -Fq 'iptables -t mangle -A eqos -d $ip -j MARK --set-mark 46' "$EQOS" ||
-    fail "configured IPv4 VIP download must install trusted mark 46"
+grep -Fq 'iptables -t mangle -A eqos -m mac --mac-source $macaddr -j CONNMARK --set-xmark 46/0xFF' "$EQOS" ||
+    fail "configured VIP must install unified CONNMARK 46"
 
-grep -Fq 'iptables -t mangle -I eqos -d 192.168.0.0/16 -m u32 --u32 "0x10&0x0000FF00=0x00006E00:0x00007700" -m u32 --u32 "0x10&0x000000FF=0x0000000A:0x00000027" -j MARK --set-mark 46' "$EQOS" ||
-    fail "static IPv4 VIP download range must install trusted mark 46"
+grep -Fq 'ip6tables -t mangle -A eqos -m mac --mac-source $macaddr -j CONNMARK --set-xmark 46/0xFF' "$EQOS" ||
+    fail "configured VIP must install unified IPv6 CONNMARK 46"
 
-grep -Fq 'ebtables -t nat -A eqos -p ipv6 -d $macaddr -j mark --mark-set 46' "$EQOS" ||
-    fail "configured IPv6 VIP download must install trusted mark 46"
+grep -Fq 'iptables -t mangle -I eqos -s 192.168.0.0/16 -m u32 --u32 "0xc&0x0000FF00=0x00006E00:0x00007700" -m u32 --u32 "0xc&0x000000FF=0x0000000A:0x00000027" -j CONNMARK --set-xmark 46/0xFF' "$EQOS" ||
+    fail "static VIP source range must install unified CONNMARK 46"
 
-grep -Fq 'ip6tables -t mangle -A eqos -m mac --mac-source $macaddr -j DSCP --set-dscp 2' "$EQOS" ||
-    fail "IPv6 hardware limit rule must set DSCP 2"
+grep -Fq 'iptables -t mangle -A eqos_apply -m mark --mark 46/0xFF -j DSCP --set-dscp 46' "$EQOS" ||
+    fail "global mark 46 to DSCP 46 translation is missing in eqos_apply"
 
-UP_LIMIT_BLOCK=$(sed -n '/if \[ \$up -ne 0 \]; then/,/fi/p' "$EQOS")
-echo "$UP_LIMIT_BLOCK" | grep -Fq 'iptables -t mangle -A eqos -s $ip -j DSCP --set-dscp 2' ||
-    fail "upload limit DSCP2 rule must only be installed when upload speed is nonzero"
+grep -Fq 'ip6tables -t mangle -A eqos -m mac --mac-source $macaddr -j CONNMARK --set-xmark 2/0xFF' "$EQOS" ||
+    fail "IPv6 hardware limit rule must set CONNMARK 2"
 
-DL_LIMIT_BLOCK=$(sed -n '/if \[ \$dl -ne 0 \]; then/,/fi/p' "$EQOS")
-echo "$DL_LIMIT_BLOCK" | grep -Fq 'iptables -t mangle -A eqos -d $ip -j DSCP --set-dscp 2' ||
-    fail "download limit DSCP2 rule must only be installed when download speed is nonzero"
+UP_LIMIT_BLOCK=$(sed -n '/if \[ \$up -ne 0 \] || \[ \$dl -ne 0 \]; then/,/fi/p' "$EQOS")
+echo "$UP_LIMIT_BLOCK" | grep -Fq 'iptables -t mangle -A eqos -m mac --mac-source $macaddr -j CONNMARK --set-xmark 2/0xFF' ||
+    fail "unified limit CONNMARK 2 rule must be installed"
 
-echo "$DL_LIMIT_BLOCK" | grep -Fq 'ebtables -t nat -A eqos -p ipv6 -d $macaddr -j mark --mark-set 2' ||
-    fail "download limit IPv6 mark2 rule must only be installed when download speed is nonzero"
-
-if grep -Eq 'ip6tables .* -A eqos .* MARK --set-mark 2' "$EQOS"; then
-    fail "IPv6 hardware limit rule still uses MARK"
+if grep -Eq 'ebtables -t nat .* eqos' "$EQOS"; then
+    fail "ebtables rules must be completely removed from eqos script"
 fi
 
-grep -Fq 'qos_mark = skb->mark;' "$HNAT_HOOK" ||
-    fail "HNAT must use exact skb mark for trusted policy markers"
+grep -Fq 'qos_mark = skb->mark & 0xFF;' "$HNAT_HOOK" ||
+    fail "HNAT must safely extract lower 8 bits of skb mark for policy matching"
 
 grep -Fq 'dir == HQOS_DOWNLOAD && qos_mark == 46' "$HNAT_HOOK" ||
     fail "HNAT must honor trusted VIP download mark 46"
@@ -65,7 +61,10 @@ grep -Fq 'qid = 32;' "$HNAT_HOOK" ||
 
 grep -Fq 'if (qos_mark == 2)' "$HNAT_HOOK" ||
     fail "HNAT must honor mark 2 as hardware limit fallback"
-
+# 检查 CONNMARK 还原规则的掩码保护以及在 eqos_apply 中的延迟应用
+RESTORE_MARK_CMD="iptables -t mangle -A eqos_apply -m conntrack --ctstate ESTABLISHED,RELATED -j CONNMARK --restore-mark --nfmask 0xFF --ctmask 0xFF"
+grep -Fq "$RESTORE_MARK_CMD" "$EQOS" ||
+    fail "eqos_apply MUST use --nfmask 0xFF --ctmask 0xFF restore-mark to apply ctmark to first packet"
 grep -Fq 'qid = (dir == HQOS_DOWNLOAD) ? 63 : 31;' "$HNAT_HOOK" ||
     fail "HNAT mark 2 fallback must map to Q31/Q63"
 
